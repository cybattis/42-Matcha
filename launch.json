{
    "version": "0.2.0",
    "configurations": [
      {
        "name": ".NET Core Launch (watch)",
        "type": "coreclr",
        "request": "attach",
        "processId": "${command:pickProcess}",
        "preLaunchTask": "watch"
      }
    ]
  }
  