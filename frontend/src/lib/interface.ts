export interface UserProfile {
  firstName: string;
  lastName: string;
  biography: string;
  coordinates: string;
  images: string[];
  tags: number[];
  isVerified: boolean;
  profilePercentage: number;
  fameRating: number;
}