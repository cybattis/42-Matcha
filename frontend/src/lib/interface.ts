export interface UserProfile {
  firstName: string;
  lastName: string;
  biography: string;
  coordinates: string;
  tags: number[];
  isVerified: boolean;
  profilePercentage: number;
  fameRating: number;
}
